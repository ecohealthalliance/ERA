template(name="loading")
  if inline
    +loading_spinner classes=classes
  else
    .loading
      +loading_spinner classes=classes

template(name="loading_spinner")
  .arc-rotate2.loading-spinner(class=classes)
    .loader
      .arc
